<UserControl x:Class="UI.WPF.Views.Forms.DetailsForm"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:UI.WPF.Views.SimProject"
             xmlns:DS="clr-namespace:DataSource;assembly=DataSource"
             xmlns:DataCnt="clr-namespace:DataInterface;assembly=DataInterface"
             xmlns:MUExt="clr-namespace:UI.WPF.Extensions"
             xmlns:LocalHelpers="clr-namespace:UI.WPF.Helpers"
             xmlns:fw="clr-namespace:SourceChord.FluentWPF;assembly=FluentWPF"
             mc:Ignorable="d" MinWidth="300"  Width="400"       
             VerticalAlignment="Center"
             d:DesignHeight="450" d:DesignWidth="800">
    
    <UserControl.Resources>
        <ResourceDictionary>
            <LocalHelpers:ConnectionTemplateSelector x:Key="ConnectionTS" />
        </ResourceDictionary>
    </UserControl.Resources>
    <Grid x:Name="MainGrid">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="5" />
            <ColumnDefinition />
            <ColumnDefinition Width="5" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="30" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition  Height="*" />
            <RowDefinition Height="30" />
        </Grid.RowDefinitions>
        <Border 
                Grid.Row="0" Grid.Column="0" CornerRadius="6,6,6,6"
                Grid.ColumnSpan="4" Grid.RowSpan="2"
               
                Background="{StaticResource BlueFormHighlight}" Margin="0,0,0,20" >
            <TextBlock Text="Details" 
                       Grid.Row="0"
                       Grid.Column="1"
                       Grid.ColumnSpan="2"
                       Margin="10,5,2,5"
                       Style="{StaticResource HeaderStyle}" />
        </Border>
        <StackPanel Orientation="Vertical"
                    VerticalAlignment="Stretch"
                    HorizontalAlignment="Stretch"                   
                    Grid.Row="2"                    
                    Grid.Column="1">
            <Image Source="{Binding SelectedNodeImage}" SourceUpdated="Image_SourceUpdated" Height="32" Width="32"  HorizontalAlignment="Left"/>
            <TextBlock Style="{StaticResource TextBlockFormStyle}">
                <TextBlock.Text>
                    <MultiBinding StringFormat="{}Key {0}">
                        <Binding Path="SelectedNode.HID.StrKey" />
                    </MultiBinding>
                </TextBlock.Text>
            </TextBlock>
            <TextBlock Text="Name"                                
                   Style="{StaticResource TextBlockFormStyle}" Margin="2,10,2,6" />
            <TextBox Text="{Binding SelectedNode.Name}"
                     Style="{StaticResource TextBoxFormStyle}" HorizontalAlignment="Left"
                     Width="{Binding RelativeSource={RelativeSource Self}, Path=MaxWidth}" />
            <TextBlock Text="Description"                                 
                   Style="{StaticResource TextBlockFormStyle}" />
        <TextBox Text="{Binding SelectedNode.Description}"
                 Grid.Row="5"
                 Grid.Column="1"
                     HorizontalAlignment="Left"
                     Style="{StaticResource TextBoxFormStyle}"
                     Width="{Binding RelativeSource={RelativeSource Self}, Path=MaxWidth}" />
        
        <TextBlock Text="{Binding Validation}" 
                       Grid.Row="6"
                       Grid.Column="1"
                   Margin="10,10,2,2"
                   Style="{StaticResource TextBlockFormStyleValidation}" />
        </StackPanel>
    </Grid>
</UserControl>
